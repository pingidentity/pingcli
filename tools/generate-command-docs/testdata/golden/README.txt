Golden files for command doc generation. Update with:
  go test -v ./tools/generate-command-docs -update

Files captured:
  pingcli.adoc - root command page (dates stripped on comparison)
  nav.adoc     - navigation structure
